# ==============================================================================
# IMPORTANT: This is a TEMPLATE file. Do NOT commit actual secrets to Git!
# ==============================================================================
# 
# To create the secrets in your IKS cluster, run:
#
#   kubectl create secret generic technova-api-secrets \
#     --from-literal=SERVICENOW_INSTANCE=<your-instance> \
#     --from-literal=SERVICENOW_USERNAME=<your-username> \
#     --from-literal=SERVICENOW_PASSWORD=<your-password> \
#     --from-literal=SLACK_BOT_TOKEN=<your-slack-token> \
#     --from-literal=SLACK_DEFAULT_CHANNEL=<your-channel> \
#     --from-literal=GITHUB_TOKEN=<your-github-token> \
#     --from-literal=GITHUB_DEFAULT_REPO=<owner/repo>
#
# To create the IBM Container Registry pull secret:
#
#   kubectl create secret docker-registry icr-io-secret \
#     --docker-server=icr.io \
#     --docker-username=iamapikey \
#     --docker-password=<your-ibm-api-key> \
#     --docker-email=<your-email>
#
# ==============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: technova-api-secrets
type: Opaque
stringData:
  # ServiceNow Configuration
  SERVICENOW_INSTANCE: "REPLACE_WITH_YOUR_INSTANCE"
  SERVICENOW_USERNAME: "REPLACE_WITH_YOUR_USERNAME"
  SERVICENOW_PASSWORD: "REPLACE_WITH_YOUR_PASSWORD"
  
  # Slack Configuration
  SLACK_BOT_TOKEN: "REPLACE_WITH_YOUR_SLACK_TOKEN"
  SLACK_DEFAULT_CHANNEL: "REPLACE_WITH_YOUR_CHANNEL"
  
  # GitHub Configuration
  GITHUB_TOKEN: "REPLACE_WITH_YOUR_GITHUB_TOKEN"
  GITHUB_DEFAULT_REPO: "REPLACE_WITH_YOUR_REPO"
